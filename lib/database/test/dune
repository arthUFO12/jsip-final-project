(library
 (name database_test)
 (libraries core types database caqti caqti-async async)
 ;; The OxCaml preview ppx_expect mis-resolves source paths from the
 ;; -source-tree-root the default backend passes; override it so expect
 ;; test corrections resolve relative to this directory.
 (inline_tests
  (flags -source-tree-root .))
 (preprocess
  (pps ppx_jane)))
